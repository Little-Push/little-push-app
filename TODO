Restore DB
Return results of POST/PUT query to client (including id field)
Guard habit PUT against modifying habits that don't belong to the user (WHERE id = 1 AND userId = 'lvahbvlha')
Only return habits for current user from habit GET
